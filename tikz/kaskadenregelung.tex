\begin{center}
    \scalebox{0.6}{
    \begin{tikzpicture}
        [
            scale = 1,
            >=latex,
            orangebox/.style={rectangle, draw=black, fill=orange, thick, minimum width=1cm, minimum height=0.5cm},
            greenbox/.style={rectangle, draw=black, fill=green, thick, minimum width=1cm, minimum height=0.5cm},
            yellowbox/.style={rectangle, draw=black, fill=yellow, thick, minimum width=1cm, minimum height=0.5cm},
            whitecircle/.style={circle, draw=black, fill=white, thick, inner sep=0pt,minimum size=0.3cm},
        ]

        % nodes
        \node               (R)                                     {$r(t)$};
        \node[greenbox]     (F)       [right= of R]                 {F};
        \node[whitecircle]  (A1)      [right= of F]                 {};
        \node[orangebox]    (C1)      [right= of A1]                {C1};   
        \node[whitecircle]  (A2)      [right= of C1]                {};
        \node[orangebox]    (C2)      [right= of A2]                {C2};   
        \node[yellowbox]    (G1)      [right= of C2]                {G1};
        \node[yellowbox]    (G2)      [right= of G1]                {G2};
        \node[yellowbox]    (H1)      [below left= of G1]           {H1};
        \node[yellowbox]    (H2)      [below= of H1]                {H2};
        \node               (U)       [right= of G2]                {$u(t)$};

        % arrows
        \draw[->, thick]    (R)         to (F.west);
        \draw[->, thick]    (F.east)    to (A1.west);
        \draw[->, thick]    (A1.east)   to (C1.west);
        \draw[->, thick]    (C1.east)   to (A2.west);
        \draw[->, thick]    (A2.east)   to (C2.west);
        \draw[->, thick]    (C2.east)   to (G1.west);
        \draw[->, thick]    (G1.east) -> node {} coordinate(G2-G1) (G2.west);
        \draw[->, thick]    (G2.east) -> node {} coordinate(U-G2) (U);
        \draw[->, thick]    (U-G2)      to (H2.east);
        \draw[->, thick]    (H2.west)   to (A1.south);
        \draw[->, thick]    (G2-G1)     to (H1.east);
        \draw[->, thick]    (H1.west)   to (A2.south);
        
    \end{tikzpicture}}
\end{center}
